# =============================================================================
# Search-Plus A/B Testing - Docker Configuration
# =============================================================================
# Copy this file to .env.local and customize with your actual values
# Priority order: .env.local > .env.docker > .env > environment variables
# =============================================================================

# =============================================================================
# Claude Code Environment Variables
# =============================================================================
# These variables are used by Claude Code in the Docker container
# All paths refer to locations inside the Docker container
# You can also set them directly if you prefer

# Standard Anthropic-compatible variables
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# ANTHROPIC_AUTH_TOKEN=your_anthropic_api_key_here
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
# ANTHROPIC_SMALL_FAST=claude-3-haiku-20240307

# Model-specific variables for different Claude Code operations
# ANTHROPIC_DEFAULT_OPUS_MODEL=claude-3-5-sonnet-20241022
# ANTHROPIC_DEFAULT_SONNET_MODEL=claude-3-5-sonnet-20241022
# ANTHROPIC_DEFAULT_HAIKU_MODEL=claude-3-haiku-20240307

# =============================================================================
# Additional Configuration Options
# =============================================================================

# Increase timeout for slow API responses (in milliseconds)
# API_TIMEOUT_MS=300000

# Disable non-essential traffic for better performance
# CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1

# =============================================================================
# Security & Usage Instructions:
# =============================================================================
#
# 🚨 SECURITY WARNING:
# - NEVER commit actual API keys to version control
# - .env.local is already in .gitignore for protection
# - Use different API keys for development vs production
# - Regularly rotate your API keys
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Edit .env.local with your actual API keys:
#    - Replace placeholder values with real credentials
#    - Ensure API keys have appropriate permissions
#    - Test API connectivity before production use
#
# 3. For automated testing:
#    node scripts/search-plus-ab-testing.mjs --real skill
#
# 4. For Docker-based testing:
#    docker-compose up --build
#
# 🔒 Recommended Security Practices:
# - Use read-only API keys when possible
# - Monitor API usage and costs
# - Keep API keys out of logs and backups
# - Use separate environments for dev/staging/prod
